# STL Anomaly Detection Service API

REST API that runs STL‑based time‑series anomaly detection and returns timestamps of outliers.

## Quickstart
```javascript
import { anomalyService } from './anomalyService.ts';
```
