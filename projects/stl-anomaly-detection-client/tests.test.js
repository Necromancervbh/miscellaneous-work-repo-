import { describe, it, expect } from 'vitest';

describe('STL Anomaly Detection Client SDK', () => {
  it('should initialize and execute without errors', () => {
    expect(true).toBe(true);
  });
});
