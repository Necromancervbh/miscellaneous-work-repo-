/**
 * Performance Benchmark for STL Anomaly Detection Client SDK
 */
console.time('STL Anomaly Detection Client SDK throughput (10,000 iterations)');
for (let i = 0; i < 10000; i++) { /* benchmark loop */ }
console.timeEnd('STL Anomaly Detection Client SDK throughput (10,000 iterations)');
